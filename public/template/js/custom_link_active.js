// link active supplier
let supplier_active = $('#supplier_active')
if(supplier_active.hasClass('active')){
    $('#collapseLayouts').addClass('show')
}

// link kategori active
let kategori_active = $('#kategori_active')
if(kategori_active.hasClass('active')){
    $('#collapseLayouts').addClass('show')
}

// link barang active
let barang_active = $('#barang_active')
if(barang_active.hasClass('active')){
    $('#collapseLayouts').addClass('show')
}

// link penjualan active
let penjualan_active = $('#penjualan_active')
if(penjualan_active.hasClass('active')){
    $('#collapseLayouts1').addClass('show')
}

// link riwayat active
let riwayat_active = $('#riwayat_active')
if(riwayat_active.hasClass('active')){
    $('#collapseLayouts1').addClass('show')
}

// link pelanggan active
let pelanggan_active = $('#pelanggan_active')
if(pelanggan_active.hasClass('active')){
    $('#collapseLayouts1').addClass('show')
}

// link barang_in active
let barang_in_active = $('#barang_in_active')
if(barang_in_active.hasClass('active')){
    $('#collapseLayouts2').addClass('show')
}

// link barang_out active
let barang_out_active = $('#barang_out_active')
if(barang_out_active.hasClass('active')){
    $('#collapseLayouts2').addClass('show')
}

// link laporan_harian active
let laporan_harian_active = $('#laporan_harian_active')
if(laporan_harian_active.hasClass('active')){
    $('#collapseLayouts3').addClass('show')
}

// link laporan_bulanan active
let laporan_bulanan_active = $('#laporan_bulanan_active')
if(laporan_bulanan_active.hasClass('active')){
    $('#collapseLayouts3').addClass('show')
}